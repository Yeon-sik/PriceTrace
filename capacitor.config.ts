import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.yeonsik.pricetracker",
  appName: "가격 추적기",
  webDir: "out",
  android: {
    backgroundColor: "#F7F8F6",
  },
};

export default config;
