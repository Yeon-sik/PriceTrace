import {describe,expect,it} from "vitest";
import {mapReceipt} from "./receipt";
import {estimateBasket,marketObservations,marketSummary,median} from "./market-analytics";
const receipt=(store:string,date:string,price:number)=>mapReceipt({receipt_metadata:{store_name:store,date,transaction_number:`${store}-${date}`,currency:"KRW",receipt_total_krw:price,item_count:1},items:[{purchase_numbers:[1],product_name:"상품",store_product_code:"P1",unit_price_krw:price,quantity:1,total_price_krw:price,name_confidence:"high"}]});
describe("market analytics",()=>{it("중앙값과 매장별 예상 장바구니를 계산한다",()=>{const receipts=[receipt("A","2026-07-01",1000),receipt("A","2026-07-02",1200),receipt("B","2026-07-02",900)];const observations=marketObservations(receipts,"P1","A");expect(median([1000,1200,1100])).toBe(1100);expect(marketSummary(observations)).toMatchObject({count:2,latest:1200,minimum:1000,maximum:1200,median:1100});expect(estimateBasket(receipts,"B",[{storeProductCode:"P1",quantity:2}]).totalKrw).toBe(1800);});});
